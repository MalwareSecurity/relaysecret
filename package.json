{
	"type": "module",
	"scripts": {
		"test": "node --test tests/*.test.js",
		"test:e2e": "playwright test",
		"check": "find worker frontend -type f -name '*.js' -print0 | xargs -0 -n1 node --check && bash -n deploy/deploy.sh deploy/lib/*.sh"
	},
	"devDependencies": {
		"@playwright/test": "^1.61.1",
		"wrangler": "^4.83.0"
	}
}
